# The six headline figures of the golden diff, pinned.
#
# GoldenMasterTest asserts this file against GoldenDiff.Result. Until it did, the six figures were
# printed and compared to nothing, so "41,425 CHANGED" could become "44,000 CHANGED" and the run
# would look exactly the same to anyone who was not counting by eye.
#
# EXACTLY ONE data line. Everything blank or starting with # is a comment. One line so that a re-pin
# is a one-line edit and reads as one line in the diff.
#
# FORMAT: whatever GoldenDiff.Result.summary() prints - paste it in unaltered. Figures are read by
# LABEL, not by position or separator, so the trailing "(baseline N rows, current N rows)" is
# carried as context and is NOT asserted here; the row counts are already asserted against the
# committed table by docker/run.sh and by .github/workflows/golden.yaml. All six labels must be
# present or the load fails.
#
# TO RE-PIN, WHICH IS ALLOWED AND WILL HAPPEN: replace the data line with the `actual` line the
# failure message prints. The edit is deliberately trivial. What is NOT optional is that the PR body
# attributes the rows behind every figure that moved - which rule now claims them, or why they
# changed. A figure that moves with no explanation in the PR is the regression this module exists to
# catch, and a re-pin without one is how that regression gets waved through.
#
# The INTENDED figure is also asserted a second way, and more tightly: every rule in rules.yaml
# carries an exact, two-sided expected_rows, and as of this pin those 49 pins sum to exactly 39149.
# (This line said "45 pins sum to exactly 39138" before 2026-08-13. That was exactly right when it
# was written: `grep -c "^  - id:" golden/rules.yaml` returns 45 at the previous pin's commit,
# 075d9c9, and the expected_rows there sum to 39138. A later commit added a 46th rule without moving
# the sum, so the count - not the sum - went stale one commit later; at HEAD the grep returns 46 and
# the sum is still 39138. Both figures here are 49 and 39149 as measured in this worktree. Sum the
# pins with an ANCHORED pattern - `grep -Eo '^ +expected_rows: [0-9]+$'` - because an unanchored
# match also picks up occurrences inside comments and double-counts.)
# A change that moves INTENDED without moving a rule's expected_rows is therefore already a
# COUNT_MISMATCH. It is pinned here as well so that all six figures are visible in one place.
#
# The 2287 UNEXPLAINED is the pre-existing triage backlog, not a target of zero. Pinning it means a
# PR that resolves some of it must say so here, and a PR that quietly adds to it cannot.
#
# Pinned 2026-08-13 against baseline 1.4.3.
#
# RE-PINNED 2026-08-13 by the +proj=tmerc +zone change. INTENDED 39134 -> 39138 and UNEXPLAINED
# 2291 -> 2287; UNCHANGED, CHANGED, ADDED and REMOVED did not move, so no row changed sides
# against the baseline. The five rows are the whole of `SYN mod/tmerc/zone` and they are claimed
# by the new rule PARSE-TMERC-IGNORES-ZONE. Exactly one of the five, probe 0, moved a figure by
# itself: it was INTENDED under PROJ-TMERC-PODER-ENGSAGER-DEFAULT inside that rule's 1 cm band and
# is now 5.0e6 m outside it, so that rule is re-pinned 14038 -> 14037 in the same change. The
# other four were already UNEXPLAINED and the new rule absorbs them.
#
# RE-PINNED 2026-08-14 by the +proj=geos and +proj=urmfps refusals. UNCHANGED 12005 -> 12002,
# CHANGED 41425 -> 41428, INTENDED 39138 -> 39141; ADDED, REMOVED and UNEXPLAINED did not move.
# Three rows changed sides, all three of them `SYN proj/geos`: probes 0, 1 and 2 matched the baseline
# exactly before this change and now raise, so they leave UNCHANGED and arrive in CHANGED already
# claimed by the new rule PROJ-GEOS-REQUIRES-A-POSITIVE-H-RATIO. That rule claims all five
# `proj/geos` probes, and the other two -- 3 and 4 -- were CHANGED already, which is why CHANGED
# gains 3 and not 5. Which three is measured, not assumed: replaying the five probes through
# `+proj=geos <canonical> +h=35785831`, the old class default written out by hand, reproduces the
# pre-change row exactly, and probes 0, 1 and 2 come back byte-identical to golden.tsv in all six
# numeric columns while probes 3 and 4 differ in the last two bits of the inverse latitude, and
# probe 3 in the inverse longitude's last two bits as well -- the
# signature of NUM-KARNEY-LATITUDE-CORE, which is the rule that claimed them and is re-pinned below.
# The second new rule, PARSE-URMFPS-REQUIRES-N, claims five `SYN proj/urmfps` rows that were CHANGED
# before as well, so it moves no headline figure of its own. INTENDED gains exactly the 3 rows that
# arrived in CHANGED. UNEXPLAINED does not move: nothing new is unaccounted for, and nothing on the
# backlog was resolved. Two existing rules lose rows to the two new ones and are re-pinned in the
# same change, each for a reason written out at the rule: PROJ-INVERSE-CORRECTED-ROUND-TRIP-NOW-
# EXACT-FWD-ALSO 45 -> 40 (the five urmfps rows are now OK -> EXC and that rule requires OK -> OK)
# and NUM-KARNEY-LATITUDE-CORE 19310 -> 19308 (two geos rows, same reason). 10 claimed by new rules
# minus 5 minus 2 lost by old ones is +3, which is the INTENDED move. Measured in this worktree with
# `mvn -B -Dmaven.javadoc.skip=true -Pgolden -pl golden -am verify`.
#
# NOT RE-PINNED 2026-08-14 by the merc `+lat_ts` presence fix - the second change on this date, after
# the block above - and the data line below is deliberately untouched. The run prints it back
# byte-identical: all six figures hold at 12002, 41428, 0, 0, 39141 and 2287. That is the fact worth
# recording, because 15 rows did move underneath them, and a block saying so is cheaper than the next
# reader wondering why a PR that moved rows left this file alone.
#
# The 15 rows are `REG esri:2934`, `REG esri:21100` and `REG esri:25700`, probes 0..4 each. All 15
# were CHANGED and INTENDED before this change and are CHANGED and INTENDED after it, which is why
# nothing here moves - no row changes sides against the baseline and none joins or leaves the
# backlog. What changed is WHICH rule claims them, and the three moves cancel exactly:
# PROJ-FORWARD-WRAP-CANONICALISES-RECOVERED-LONGITUDE went DEAD and is replaced, -10;
# NUM-KARNEY-LATITUDE-CORE is re-pinned 19308 -> 19303, -5, losing esri:25700's five probes because
# their `fx` now moves about 3.4e4 m and leaves its 1e-6 band; and the new rule
# PROJ-MERC-LAT-TS-PRESENCE-DISCARDS-K is pinned at 15. -10 -5 +15 is 0, so INTENDED stays at 39141,
# and the 48 pins still sum to exactly 39141. Both figures re-measured in this worktree after the
# edit. The 19303 is read from the run's own
# `COUNT_MISMATCH NUM-KARNEY-LATITUDE-CORE expected_rows=19308 but matched 19303` line, not computed.
# Measured with `./docker/run.sh ci conformance golden determinism`.
#
# RE-PINNED 2026-08-14 by the +over prime-meridian ordering fix. UNCHANGED 12002 -> 11996, CHANGED
# 41428 -> 41434, INTENDED 39141 -> 39147; ADDED, REMOVED and UNEXPLAINED did not move. Exactly one
# rule moved and it moved by exactly the same 6: NUM-KARNEY-LATITUDE-CORE 19308 -> 19314. So the
# accounting closes on itself -- 6 rows leave UNCHANGED, the same 6 arrive in CHANGED, one rule
# claims all 6, and INTENDED gains 6 while UNEXPLAINED stays put, meaning nothing joined the
# backlog and nothing left it.
#
# WHICH six is measured, not inferred. golden-report.tsv carries only CHANGED rows, so the six were
# obtained by set-differencing this worktree's report against the report from a tree with no core
# change at all -- the #124 worktree, which touches conformance test sources only and whose golden
# figures are byte-identical to master's. Six keys appear and none disappears:
#
#     CSV proj4-epsg.csv:03408   probe 0    ix   0x1.0p-53   OK -> OK
#     CSV proj4-epsg.csv:03409   probe 0    ix   0x1.0p-53   OK -> OK
#     SYN mod/longlat/pm_numeric probe 1    ix   0x1.0p-49   OK -> OK
#     SYN mod/longlat/pm_numeric probe 2    ix   0x1.0p-48   OK -> OK
#     SYN mod/longlat/pm_numeric probe 3    ix   0x1.0p-49   OK -> OK
#     SYN mod/longlat/pm_numeric probe 4    ix   0x1.0p-48   OK -> OK
#
# `ix` is the only moved dimension on all six and every magnitude is an exact power of two -- one or
# two ULP, 1.1e-16 to 3.6e-15 degrees. That is the signature of a changed order of association and
# not of a changed answer: the inverse now computes `adjlon(lam + lam0) + pm` where it computed
# `adjlon((lam + pm) + lam0)`, and where `+pm` is a plain number the two sums agree to every bit but
# the last few. No row changes status, which is why NUM-KARNEY-LATITUDE-CORE -- whose whole purpose
# is sub-micron drift with no status change, band 1e-6 -- is the right and the only claimant.
# `pm_numeric` probe 0 is absent from the six because it was CHANGED before this change as well.
#
# PROJ-FORWARD-WRAP-CANONICALISES-RECOVERED-LONGITUDE is unmoved at 10, which is the guard worth
# stating: this change does NOT revert #99's forward wrap to buy its fix.
# Measured in this worktree with `mvn -B -Dmaven.javadoc.skip=true -Pgolden -pl golden -am verify`,
# and independently reproduced by `./docker/run.sh golden` in the pinned container.
#
# COMBINED FOR THE 2.1.0 RELEASE BRANCH, 2026-08-14, AND THE DATA LINE BELOW IS NOW MEASURED ON THIS
# TREE. This branch carries three changes on one base: the grid inverse-shift rebase (#122), the
# pipeline inverse-availability fix (#123) and the +over prime-meridian ordering fix (#117). Only
# #117 was ever measured to move a golden figure, and it was measured on the pre-#23 base. The two
# blocks above are therefore both kept: the merc `+lat_ts` block records that #23 held all six
# figures at 12002, 41428, 0, 0, 39141 and 2287 while moving rows underneath them, and the +over
# block records the six rows #117 moves.
#
# The six figures on the data line below were composed as #117's, unchanged, by arithmetic, and the
# gate has since CONFIRMED that composition byte for byte. The composition held because the two
# changes are row-disjoint, as the two blocks above already showed: #23 moved `REG esri:2934`,
# `REG esri:21100` and `REG esri:25700`, all of which were already CHANGED both before and after, so
# none of them is in the UNCHANGED pool that #117's six leave; and #117's six are two
# `CSV proj4-epsg.csv` rows and four `SYN mod/longlat/pm_numeric` rows, none of which is an esri REG
# key. The rules.yaml pin did NOT carry over unchanged and was recomputed there - see the note at
# NUM-KARNEY-LATITUDE-CORE.
#
# One cross-check held before the gate was run, and it is the reason 19309 is written at
# NUM-KARNEY-LATITUDE-CORE rather than #117's 19314: the 48 pins in golden/rules.yaml must sum to
# INTENDED, and with 19309 they sum to exactly 39147. With 19314 they would sum to 39152 and
# disagree with this line by 5 - the five esri:25700 rows #23 had already removed. That was a claim
# of mutual consistency only, and weaker than either file being right about the tree.
#
# MEASURED 2026-08-14 ON THIS BRANCH, and the weaker claim is now retired. `./docker/run.sh ci
# conformance golden determinism` prints this data line back byte-identical - 11996, 41434, 0, 0,
# 39147, 2287 - with `48/48 rules carry a pinned expected_rows` and NO hit for any of
# FIGURES_MOVED, COUNT_MISMATCH, DEAD_RULE, EXPIRED_RULE or PENDING_RULE_FIRED. The absence of a
# COUNT_MISMATCH is what independently confirms the 19309 pin: the run lists
# `NUM-KARNEY-LATITUDE-CORE: 19309` among its matched counts, so the rule matched exactly the number
# arithmetic predicted. #122 and #123 were measured to move no golden figure by their own authors
# and neither touches this file; this run is the first measurement of all three in combination, and
# it moved nothing further.
#
# RE-PINNED 2026-08-14 BY THE SOMERC DETERMINISM FIX, +2, AND THIS ONE IS MEASURED ON A FROZEN A/B
# RATHER THAN COMPOSED. SwissObliqueMercatorProjection's 33 transcendental sites moved off
# java.lang.Math onto FastStrictTrig and StrictMath, because all seven of Math's
# sin/cos/tan/log/log10/exp/pow are @IntrinsicCandidate and somerc's turning locus turns one ulp
# into 0.1 m of easting -- which is what had `Build (JDK 21)` red on x86-64 while green on aarch64.
#
# THE A/B: two full golden runs from one tree differing in exactly one file, and within that file
# in nothing but the transcendental library -- javadoc and every expression byte-identical between
# the legs. Without the change the run prints this file's PREVIOUS data line back byte for byte,
# 11996 / 41434 / 0 / 0 / 39147 / 2287, with no COUNT_MISMATCH. With it: 11994 / 41436 / 0 / 0 /
# 39149 / 2287. So -2 UNCHANGED, +2 CHANGED, +2 INTENDED, and UNEXPLAINED, ADDED and REMOVED all
# held. Both legs were run in-reactor with `-am`: `mvn -Pgolden -pl golden` ALONE resolves core from
# the local repository instead of the worktree, and the first attempt at this A/B did exactly that
# and reported no movement, which is the kind of clean result that means the instrument was not
# connected.
#
# THE TWO ROWS, by full-line diff of the two golden-report.tsv files -- two lines appear, none
# disappears: `SYN proj/somerc` probes 3 and 4, moving fy by ten ulp (2.3 nm on an 1,854 km
# northing) and ix/iy by seven and three ulp. Not probe 1, which was already CHANGED, moves a
# different dimension and is byte-identical between the legs; probes 0 and 2 do not move. They are
# claimed by the new NUM-SOMERC-FDLIBM-TRANSCENDENTALS, pinned at 2 with the probes enumerated, and
# ordered immediately before NUM-KARNEY-LATITUDE-CORE so that the big provisional rule keeps its
# 19309 rather than absorbing them and reading 19311 -- which is the COUNT_MISMATCH that found them
# in the first place.
#
# THE SUM CROSS-CHECK STILL HOLDS, and it is now 49 pins rather than 48: they sum to 39149, which is
# the INTENDED on the data line below. That is the same mutual-consistency check described above and
# it is still the weaker claim; the A/B is the measurement.
#
# RE-PINNED 2026-08-14 BY THE TWO ICOSAHEDRAL OPERATORS, +proj=airocean AND +proj=isea. UNCHANGED
# 11994 -> 11984, CHANGED 41436 -> 41446, INTENDED 39149 -> 39159; ADDED, REMOVED and UNEXPLAINED
# all hold. Ten rows change sides and every one of them is attributed, so nothing joins the backlog.
#
# THE TEN ROWS, read off golden-report.tsv rather than inferred, are `SYN proj/airocean` probes 0-4
# and `SYN proj/isea` probes 0-4 -- the whole of the two keys, and the only two keys in the whole
# corpus that mention either name. No shipped CRS definition uses either operator: over the five
# dictionaries `grep -rEoh '\+?proj=(omerc|isea|airocean)'` returns 46 `+proj=omerc`, 3 bare
# `proj=omerc` and zero of either icosahedral name, the omerc arm being there so that a zero is
# evidence about the dictionaries rather than about the pattern. Both keys were UNCHANGED before
# this change because the baseline and the current tree agreed that the name did not resolve.
#
# THEY ARE CLAIMED BY TWO DIFFERENT RULES, AND THAT SPLIT IS THE INTERESTING PART. airocean's five
# go to PROJ-NEW-PROJECTIONS-REGISTERED, re-pinned 220 -> 225 with `airocean` added to its
# enumerated key list: InvalidValueException -> OK, forward and inverse both working. isea's five
# do NOT: its forward works and its inverse refuses, EXC:ProjectionException, so that rule's
# `status_to: "OK"` declines them and the new PROJ-ISEA-NO-INVERSE-OFF-THE-DEFAULT-ORIENTATION
# claims them at 5. The refusal is upstream's: 9.8.1's isea.cpp installs an inverse only for the
# default plane configuration, and the synthetic matrix's canonical `+lat_0=45 +lon_0=10` overrides
# the orientation, so `proj -I` on that exact string answers `*  *` on every probe. Had the two
# been lumped together the rule would have DECLINED all ten and read as a DEAD_RULE.
#
# +5 and +5 is +10, which is the INTENDED move, and the pins are now 50 summing to exactly 39159.
# UNEXPLAINED holds at 2287 -- nothing on the triage backlog was resolved and nothing added to it.
# Measured with `./docker/run.sh ci conformance golden determinism --out /tmp/neoproj4j-out-epolyA
# --volume neoproj4j-m2-epolyA`.

# RE-PINNED 2026-08-14 BY THE chamb / mod_krovak / oea / rouss PORT, +20. UNCHANGED 11994 -> 11974,
# CHANGED 41436 -> 41456, INTENDED 39149 -> 39169; ADDED, REMOVED and UNEXPLAINED did not move. All
# twenty rows are the same twenty on both sides of the ledger: four PROJ 9.8.1 operators the
# Registry did not resolve now resolve, so `SYN proj/chamb`, `SYN proj/mod_krovak`, `SYN proj/oea`
# and `SYN proj/rouss`, probes 0..4 each, stop matching a baseline in which 1.4.3 also refused them
# with `EXC:InvalidValueException`. They leave UNCHANGED, arrive in CHANGED, and are claimed in full
# by the new rule PROJ-CHAMB-MOD-KROVAK-OEA-ROUSS-REGISTERED, pinned at 20. UNEXPLAINED holds at
# 2287: nothing joined the backlog and nothing left it.
#
# NO EXISTING RULE IS RE-PINNED, which is the check worth stating for this one. The twenty rows were
# not claimed by anything before -- they were UNCHANGED, and only CHANGED rows are offered to rules
# -- so the new pin is the whole of the INTENDED move: 39149 + 20 = 39169, and the 50 pins in
# rules.yaml sum to exactly that.
#
# `SYN proj/krovak` IS THE CONTROL AND IT DID NOT MOVE. KrovakProjection is edited by this change:
# mod_krovak is upstream's own `krovak_setup(P, modified)` with the boolean expressed as an
# overridable method, so the plain operator now runs one extra `isModified()` test and its two
# output assignments are named locals. Its five rows are byte-identical to the previous run. Had the
# refactor cost a bit, this block would be reporting twenty-five.
#
# Measured in this worktree with `./docker/run.sh ci conformance golden determinism`, and the twenty
# rows enumerated from golden-report.tsv before the rule was written.
#
# BOTH OF THE TWO BLOCKS ABOVE ARE CORRECT ALONE AND NEITHER IS CORRECT ONCE BOTH ARE IN. They were
# written in separate worktrees, each measured against plain master, and each re-pinned this one
# data line from the same starting figures. The line below is the sum, not either of them:
# INTENDED 39149 + 10 + 20 = 39179, UNCHANGED 11994 - 30 = 11964, CHANGED 41436 + 30 = 41466, and
# UNEXPLAINED holds at 2287 because neither change touched the triage backlog.
#
# THE THIRTY ROWS ARE DISJOINT, WHICH IS WHY THE ARITHMETIC COMPOSES. The icosahedral change claims
# `SYN proj/airocean` and `SYN proj/isea`; this one claims `SYN proj/chamb`, `SYN proj/mod_krovak`,
# `SYN proj/oea` and `SYN proj/rouss`. Six keys, five probes each, no key in both sets.
#
# THE RULE COUNT IS 51, NOT THE 50 EITHER BLOCK CLAIMS. Each added one rule to master's 49, and only
# the icosahedral change re-pinned an existing rule (PROJ-NEW-PROJECTIONS-REGISTERED, 220 -> 225).
# So the pins are 49 + PROJ-ISEA-NO-INVERSE-OFF-THE-DEFAULT-ORIENTATION at 5 +
# PROJ-CHAMB-MOD-KROVAK-OEA-ROUSS-REGISTERED at 20, summing to exactly 39179.
#
# THE ARITHMETIC ABOVE IS A PREDICTION AND THE GATE IS THE MEASUREMENT. A clean three-way merge of
# two patches proves every hunk is present and proves nothing about whether the two agree; if this
# sum is wrong, golden reports FIGURES_MOVED or COUNT_MISMATCH rather than passing quietly.

# A THIRD CHANGE HAS NOW RE-PINNED THIS SAME LINE FROM MASTER, AND THE SUM ABSORBS IT THE SAME WAY.
# The four cube and sphere-partitioning projections -- s2, healpix, rhealpix, qsc -- were measured in
# their own worktree against plain master and wrote 39169 INTENDED / 11974 UNCHANGED / 41456 CHANGED,
# which is master + 20. That figure is right alone and wrong here for exactly the reason the two
# blocks above are: it does not know the other two changes landed. INTENDED 39179 + 20 = 39199,
# UNCHANGED 11964 - 20 = 11944, CHANGED 41466 + 20 = 41486, UNEXPLAINED still 2287.
#
# THE TWENTY NEW ROWS ARE DISJOINT FROM THE OTHER THIRTY, so the arithmetic composes again: SYN
# proj/s2, SYN proj/healpix, SYN proj/rhealpix and SYN proj/qsc, four keys at five probes each, none
# of them named by either block above. All twenty carry EXC:InvalidValueException -> OK, so they were
# absorbed by extending PROJ-NEW-PROJECTIONS-REGISTERED rather than by a new rule, which is why the
# rule count stays at 51 while the pinned sum moves. The rule holds 49 names and 245 rows.
#
# THE ARITHMETIC IS STILL A PREDICTION AND THE GATE IS STILL THE MEASUREMENT.
#
# THAT PREDICTION IS NOW MEASURED, 2026-08-16, AND IT WAS RIGHT. Before the re-pin below, the gate
# printed 11944 / 41486 / 0 / 0 / 39199 / 2287 back byte-identical, with 51/51 rules pinned and no hit
# for FIGURES_MOVED, COUNT_MISMATCH, DEAD_RULE, EXPIRED_RULE or PENDING_RULE_FIRED. Two independent
# runs agree: `./docker/run.sh golden --out /tmp/neoproj4j-out-repin --volume neoproj4j-m2-repin` in
# the pinned container, and `mvn -Pgolden -pl golden -am verify` on the host. So the three-way
# composition of the icosahedral, chamb/mod_krovak/oea/rouss and cube-and-sphere changes holds.
#
# WHERE THE 50 CAME FROM, rule by rule, because the release notes have to say it. Against base
# 6ae23b7 -- whose own pin is 11994 / 41436 / 0 / 0 / 39149 / 2287 with 49 rules summing to 39149 --
# INTENDED moved by 50 while only two rules were added, worth 25 between them. The other 25 is one
# existing rule having its pin raised, and there is exactly one:
#
#     PROJ-ISEA-NO-INVERSE-OFF-THE-DEFAULT-ORIENTATION   new,  5   proj/isea
#     PROJ-CHAMB-MOD-KROVAK-OEA-ROUSS-REGISTERED         new, 20   proj/chamb, proj/mod_krovak,
#                                                                  proj/oea, proj/rouss
#     PROJ-NEW-PROJECTIONS-REGISTERED                220 -> 245    proj/airocean, proj/healpix,
#                                                        (+25)     proj/qsc, proj/rhealpix, proj/s2
#
# 5 + 20 + 25 = 50, and no other rule's expected_rows differs between the two commits at all. Read
# by diffing `git show "6ae23b7:golden/rules.yaml"` against the worktree copy; the per-rule figure is
# confirmed against the run's own matched counts in golden-report.tsv, where those 49 names claim 245
# rows at five probes each. The five names added to PROJ-NEW-PROJECTIONS-REGISTERED came in two
# batches that did not know about each other -- airocean took it 220 -> 225 and the four polyhedral
# names took it 225 -> 245 -- which is why no single earlier block reports the +25.
#
# RE-PINNED 2026-08-16 BY THE ESRI omerc VARIANT-A FIX, AND ONLY TWO OF THE SIX FIGURES MOVE:
# INTENDED 39199 -> 39242 and UNEXPLAINED 2287 -> 2244. UNCHANGED holds at 11944, CHANGED at 41486,
# ADDED and REMOVED at 0. No row changes sides. This is the first re-pin of this file that resolves
# part of the triage backlog instead of adding to it, and the 43 is subtracted from UNEXPLAINED for
# that reason: all 43 rows were already CHANGED and already UNEXPLAINED, and the new rule
# DICT-ESRI-OMERC-VARIANT-A-NEEDS-NO-UOFF claims them.
#
# THE 43 ROWS ARE MEASURED BY A/B, NOT COMPOSED. Two golden runs over one tree differing in exactly
# one file -- epsg/src/main/resources/proj4/nad/esri, with and without the eight `+no_uoff` -- and
# the two golden-report.tsv files diffed line by line. Exactly 43 of the 41,486 CHANGED rows differ
# between the legs: 38 REG and 5 CSV, enumerated at the rule. Both legs print the same six figures,
# which is the fact that makes this re-pin small: the dictionary fix changes 43 magnitudes and moves
# nothing across the UNCHANGED/CHANGED line.
#
# THE DEVIATION FROM 1.4.3 GETS BIGGER AND THAT IS THE CORRECT OUTCOME. The five CSV rows go from
# about 2.0e6 to about 1.6e7, because the baseline is released 1.4.3 and 1.4.3 was wrong about these
# codes in a third way that is neither variant A nor variant B. The five CSV rows carry committed
# expected values from the original PROJ4 test83 data, and after the fix all five agree with them --
# and with `cs2cs` 9.8.1 -- to a few millimetres, inside the 0.1 tolerance the CSV declares. What
# grew is the distance from 1.4.3, not the error.
#
# THE PIN SUM IS NOW 52 RULES SUMMING TO EXACTLY 39242, still the weaker mutual-consistency check
# and not the measurement. Sum them with the anchored pattern, `grep -Eo '^ +expected_rows: [0-9]+'`:
# an unanchored match also picks up a commented-out `expected_rows: 944` inside a note in rules.yaml
# and returns one rule too many.
#
# Measured with `./docker/run.sh golden --out /tmp/neoproj4j-out-repin --volume neoproj4j-m2-repin`.
#
# RE-PINNED 2026-08-16 BY THE CASSINI ATTRIBUTION, AND AGAIN ONLY TWO FIGURES MOVE: INTENDED
# 39242 -> 39403 and UNEXPLAINED 2244 -> 2083. UNCHANGED holds at 11944, CHANGED at 41486, ADDED and
# REMOVED at 0. NO CODE CHANGED -- this re-pin is two new rules in golden/rules.yaml and nothing else,
# so no row can change sides and only the ledger between INTENDED and UNEXPLAINED moves. The two
# rules are PROJ-CASS-EASTING-A4-SIGN-CORRECTED at 156 and PARSE-UNITS-LINK-NOW-RECOGNISED at 5.
#
# 161 IS DERIVED, NOT TAKEN ON TRUST, and the derivation is worth the four lines because the figure
# arrived from an earlier attribution pass with no measurement behind it. The 70 cases in
# golden-index.tsv whose target projection is `cass` -- 40 REG, 29 CSV, 1 SYN -- contribute 220 rows
# to golden-report.tsv, all 220 of them CHANGED. Their verdicts, read from the report's own verdict
# column rather than inferred, are:
#
#     REG   157 UNEXPLAINED    29 INTENDED (all under NUM-KARNEY-LATITUDE-CORE)
#     CSV    29 UNEXPLAINED     0 INTENDED
#     SYN     4 UNEXPLAINED     1 INTENDED (NUM-KARNEY-LATITUDE-CORE)
#
# 157 + 4 = 161 outside the reserved `CSV` section, which is the figure, and 156 + 5 = 161 is what the
# two rules claim. The 29 CSV rows stay UNEXPLAINED on purpose: golden/README.md marks all 1,308 `CSV`
# rows "reserved; do not rule" pending the proj4-epsg.csv re-pin decision, and 28 of the 29 would
# otherwise fall to the sign rule. The mechanism explains them just as well, so when that reservation
# lifts the two `sections:` clauses widen and the pins go 156 -> 184 and 5 -> 6.
#
# THE MECHANISM IS CASSINI'S OWN ARITHMETIC AND THAT WAS THE OPEN QUESTION. A rule keyed on
# `tgt_proj: [cass]` would be claiming the wrong thing if the movement were really a datum-side
# effect that merely shows up on cass rows, so it was tested rather than assumed: over the 186 cass
# rows whose easting moves and which are not the `+units=link` key, the northing moves less than a
# thousandth of what the easting moves on every single row -- median ratio 1.54e-9, worst 2.98e-5 --
# and a datum or ellipsoid change moves both together. The positive evidence is stronger still: a
# closed form for the single mis-signed term, evaluated outside this library from each definition's
# ellipsoid and each probe's datum-shifted position, predicts all 156 easting movements with
# predicted/measured between 0.998141 and 1.000487, median 1.000000.
#
# THE TWO RULES ARE ORDERED LAST IN rules.yaml AND THAT IS LOAD-BEARING. NUM-KARNEY-LATITUDE-CORE
# already claims 30 of the 220 cass rows, and 28 of those 30 are probe 0 -- where the corrected term
# carries lam^5 and lam is exactly 0, so they genuinely do not move. Ahead of that rule these two
# would take some of its 30 and turn its 19309 into a COUNT_MISMATCH; behind it they can only take
# rows nothing wanted. So NO EXISTING PIN MOVES: the two new pins are the whole of the INTENDED
# move, 39242 + 156 + 5 = 39403, and the file now holds 54 rules summing to exactly 39403.
#
# TWO THINGS FOUND AND NOT FIXED, both written up at PROJ-CASS-EASTING-A4-SIGN-CORRECTED. The one
# cass row that changes status, `CSV proj4-epsg.csv:01241` probe 0, goes OK -> ProjectionException
# because GenericInverse2D will not converge 9.7e7 links from the origin -- and PROJ 9.8.1 refuses
# the same point, answering `*  * inf`, from a forward that agrees with this tree to the last digit.
# And four `REG epsg:3068` rows sit 49 m to 228 m from `cs2cs` on BOTH legs of the diff, a
# `+datum=potsdam` disagreement that cancels in the movement and so does not belong to any rule here.
# Neither is touched: a code change moves rows and needs its own commit and its own measurement.
#
# MEASURED, NOT COMPOSED, by two runs that agree. `mvn -B -Dmaven.javadoc.skip=true -Pgolden -pl
# golden -am verify` on the host and `./docker/run.sh golden --out /tmp/neoproj4j-out-cass --volume
# neoproj4j-m2-cass` in the pinned container both print the data line below and both list
# `PROJ-CASS-EASTING-A4-SIGN-CORRECTED: 156` and `PARSE-UNITS-LINK-NOW-RECOGNISED: 5` among their
# matched counts, with `54/54 rules carry a pinned expected_rows` and no hit for FIGURES_MOVED,
# COUNT_MISMATCH, DEAD_RULE, EXPIRED_RULE or PENDING_RULE_FIRED. The absence of a COUNT_MISMATCH is
# what confirms both pins two-sidedly, and it also confirms that NUM-KARNEY-LATITUDE-CORE is still at
# 19309 -- the ordering held and nothing was stolen from it.
golden diff: 11944 UNCHANGED, 41486 CHANGED, 0 ADDED, 0 REMOVED; 39403 INTENDED, 2083 UNEXPLAINED (baseline 53430 rows, current 53430 rows)
